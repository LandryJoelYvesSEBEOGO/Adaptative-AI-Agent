# Web Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
httpx==0.27.2

# CORS et sécurité
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt>=4.0.1,<5.0.0  # Requis par chromadb, patch appliqué pour passlib
python-dotenv==1.0.0

# Validation et sérialisation
pydantic[email]
pydantic-settings==2.1.0

# Utilitaires
aiofiles==23.2.1
pytest==7.4.0
tqdm>=4.66.0  # Barres de progression

# LangChain et dépendances
langchain>=0.3.0
langchain-groq>=0.1.0
langchain-community>=0.0.20
langchain-core>=0.1.0
langchain-nomic>=0.0.1
langchain-text-splitters>=0.0.1  # Text splitters (déplacé depuis langchain dans v0.3.0+)

# Base de données vectorielle
chromadb>=0.4.0

# Recherche web
tavily-python>=0.3.0

# Extraction de métadonnées avec modèles locaux
keybert>=0.8.0  # Extraction de mots-clés
spacy>=3.7.0  # NER (Named Entity Recognition)
bertopic>=0.16.0  # Détection de sujets
transformers>=4.35.0  # Modèles T5/Pegasus pour résumés
sentence-transformers>=2.2.0  # Embeddings pour BERTopic
nltk>=3.8.0  # Tokenisation (optionnel, fallback disponible)
torch>=2.0.0  # PyTorch pour les modèles transformers